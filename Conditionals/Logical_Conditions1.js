// Predict the output of the following code:

if (false || true) {
  console.log('Yes!');
} else {
  console.log('No...');
}

// The output will be Yes! Since the || operator will return the true condition if either expression in the if
// statement evaluates as true